// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true, // Ignora todos os erros de ESLint durante o build
  },
};

module.exports = nextConfig;
